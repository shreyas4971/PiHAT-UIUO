**1. WIN-PiHAT-16DI.py**
A continuous monitoring loop that reads the real-time state of all 16 digital input channels. The script utilizes internal pull-up resistors and active-LOW logic to detect external high-voltage signals (e.g., 5V/12V/24V upto 80V max. sensor triggers), immediately logging any active inputs to the terminal.
